{
	// General sync settings like the location
	"sync": true,
	"sync_folder": "",

	// Files to include in the sync, as long they do not mach the ignore pattern
	"include_pattern": [
		"*.sublime-build",
		"*.sublime-keymap",
		"*.sublime-settings"
	],

	// Files to ignore when syncing, by default Package Syncing.sublime-settings is
	// alsways excluded
	"ignore_pattern": [
		"*.DS_Store",
		"Package Control.cache*"
	]
}